import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crunch.calculator',
  appName: 'Crunch Calculator',
  webDir: 'dist'
};

export default config;
